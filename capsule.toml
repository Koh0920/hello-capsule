schema_version = "0.2"
name = "hello-capsule"
version = "1.0.4"
description = "A minimal sample app demonstrating Capsule app structure"
type = "app"
default_target = "static"

[targets.static]
runtime = "web"
driver = "static"
entrypoint = "."
port = 4173

[metadata]
repository = "Koh0920/hello-capsule"
